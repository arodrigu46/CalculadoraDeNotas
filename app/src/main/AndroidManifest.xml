<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ziron.calculadoradenotas">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Principal" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Notas" />
        <activity android:name=".Nivel" />
        <activity android:name=".Promedio" />
        <activity android:name=".Porcentajes" />
        <activity android:name=".Notas2" />
        <activity android:name=".ImgUpload" />
        <activity android:name=".CrearTarea" />
        <activity
            android:name=".PrincipalTarea"
            android:label="@string/title_activity_principal_tarea"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".IntroTareas" />
        <activity android:name=".Chat"></activity>
    </application>

</manifest>